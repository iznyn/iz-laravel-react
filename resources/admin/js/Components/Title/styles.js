/**
 * Components/Title/styles.js
 */
import { createUseStyles } from 'react-jss';

const config = {
  base: {
    position: 'relative',
  },
};

export default createUseStyles(config, { name: 'Title' });
